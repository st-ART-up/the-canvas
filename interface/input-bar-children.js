const blessed = require('blessed');
const { inputContainer } = require('./startup-screen');

//input container childern go here when complete

  module.exports = {
      inputDirectionButton,
      inputBar
  }